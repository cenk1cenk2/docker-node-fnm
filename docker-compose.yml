version: '3.7'
services:
  monorepo:
    image: cenk1cenk2/node-fnm:test
    build:
      context: .
    environment:
      LOG_LEVEL: trace
      DEFAULTS_LOGS: 'true'
      SERVICE_0_CWD: supervisor
    volumes:
      - .:/data
