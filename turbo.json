{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "templates/**", "bin/**"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "inputs": ["src/**"]
    },
    "format": {
      "inputs": ["src/**"]
    }
  }
}
